# Краткий справочник разработчика

## Быстрые исправления ошибок

### Ошибка: `Bad state: No ProviderScope found`
**Файл:** `lib/main.dart`
```dart
runApp(const ProviderScope(child: LezhandrApp()));
```

### Ошибка: `locale ru_RU is not supported`
**Файл:** `pubspec.yaml` — добавить:
```yaml
flutter_localizations:
  sdk: flutter
```
**Файл:** `lib/app.dart` — добавить:
```dart
localizationsDelegates: const [
  GlobalMaterialLocalizations.delegate,
  GlobalWidgetsLocalizations.delegate,
  GlobalCupertinoLocalizations.delegate,
],
```

### Ошибка: `type 'Null' is not a subtype of type 'int'`
**Решение:** Все модели данных должны использовать nullable типы и default значения:
```dart
factory MyModel.fromJson(Map<String, dynamic> json) {
  return MyModel(
    id: json['id'] as int? ?? 0,
    name: json['name'] as String? ?? '',
  );
}
```

### Ошибка: `Cannot access property on potentially null`
**Решение:** Добавить safe getter или использовать `?.`:
```dart
String get sourceName => source?.name ?? 'Unknown';
```

### Ошибка: Символы $ интерпретируются как string interpolation
**Решение:** Использовать raw string:
```dart
// ❌ Неправильно
const hint = 'Текст условия (поддержка LaTeX: $...$ или $$...$$)';

// ✅ Правильно
const hint = r'Текст условия (поддержка LaTeX: $...$ или $$...$$)';
```

---

## Архитектура проекта

```
lib/
├── main.dart              # ProviderScope обёртка
├── app.dart               # MaterialApp.router + локализация
├── core/                  # Общие компоненты
│   ├── config/            # AppConfig (API URL)
│   ├── theme/             # Material 3 тема
│   ├── router/            # GoRouter маршруты
│   └── motivation/        # Движок мотивации
├── data/                  # Слой данных
│   ├── models/            # DTO (без json_serializable!)
│   ├── repositories/      # API вызовы
│   ├── services/          # ApiClient (Dio)
│   └── storage/           # TokenStorage, DeviceStorage
└── presentation/          # UI слой
    ├── providers/         # Riverpod StateNotifier
    ├── screens/           # Виджеты экранов
    └── widgets/           # Переиспользуемые компоненты
```

---

## Текущие TODO

### Критические (P1)
| Задача | Описание | Файл |
|--------|----------|------|
| 🔴 BUG-AUTH-001 | Авто-переподключение при истечении токена | `auth_provider.dart` |
| 🔴 BUG-COMM-001 | Фильтрация активных задач по user_id | `solutions_provider.dart` |

### UI/UX (P1.5)
| Задача | Описание | Файл |
|--------|----------|------|
| 🟡 BUG-UI-001 | Улучшение экрана первого входа | `login_screen.dart` |

### Низкий приоритет (P2)
| Задача | Описание |
|--------|----------|
| 🟢 Анализ концепций | UI для AI-анализа задач |
| 🟢 Комментарии | UI комментариев к задачам |
| 🟢 Финансы | UI пополнения баланса |
| 🟢 Переключение темы | Связать toggle с темой |
| 🟢 Уведомления | Push-уведомления |

---

## Реализованные функции

### ✅ Авторизация
- Device login (автосоздание аккаунта)
- Логин по email/пароль
- Конвертация анонимного аккаунта

### ✅ Задачи
- Просмотр библиотеки с фильтрацией
- Создание задачи
- Редактирование условия и тегов
- OCR с выбором AI-персоны
- Просмотр фото с zoom

### ✅ Решения
- Создание и сессии с таймером
- Завершение с оценками
- Продолжение активной сессии
- OCR решений

### ✅ Артефакты
- Озарения (создание, фото)
- Вопросы (создание, ответы, AI-ответы)
- Подсказки (черновик, AI-генерация)

### ✅ UI
- LaTeX поддержка ($...$ и $$...$$)
- Markdown рендеринг
- Выбор AI-персоны

---

## Запуск

```bash
flutter pub get
flutter run -d linux
```

---

## Важно помнить

1. **Не использовать json_serializable** — только ручной fromJson/toJson
2. **Все поля из API — nullable** — API может вернуть null
3. **ProviderScope обязателен** — Riverpod не работает без него
4. **Локализация требует delegates** — иначе краш на ru_RU
5. **Raw strings для LaTeX** — используйте `r'...'` для строк с $

---

## Ключевые файлы

| Назначение | Файл |
|------------|------|
| Авторизация | `lib/presentation/providers/auth_provider.dart` |
| Задачи | `lib/presentation/providers/problems_provider.dart` |
| Решения | `lib/presentation/providers/solutions_provider.dart` |
| Артефакты | `lib/presentation/providers/artifacts_provider.dart` |
| OCR | `lib/presentation/providers/ocr_provider.dart` |
| Детали задачи | `lib/presentation/screens/problems/problem_detail_screen.dart` |
| Сессия решения | `lib/presentation/screens/solutions/solution_session_screen.dart` |
| Библиотека | `lib/presentation/screens/library/library_screen.dart` |
| LaTeX/Markdown | `lib/presentation/widgets/shared/markdown_with_math.dart` |
| AI Персоны | `lib/presentation/widgets/shared/persona_selector.dart` |

---

## Документация

| Документ | Назначение |
|----------|------------|
| `PROJECT_STATUS.md` | Статус багов и функционала |
| `IMPLEMENTATION_PLAN.md` | План реализации |
| `CHANGELOG.md` | История изменений |
| `KODA.md` | API документация сервера (в upload/) |
