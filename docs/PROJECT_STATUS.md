# Статус проекта — Лежандр

_Последнее обновление: 2026-02-24_

## Краткая сводка

| Категория | Исправлено | Открыто |
|-----------|------------|---------|
| Критические баги | 5 | 0 |
| Функциональные баги | 12 | 0 |
| UI/UX проблемы | 6 | 0 |

---

## Открытые баги


### BUG-UI-002: Неработающие переключатели настроек
**Приоритет:** 🟢 Низкий
**Статус:** ❌ Открыт

**Список неработающих элементов:**

| Кнопка | Экран | Статус |
|--------|-------|--------|
| Уведомления | ProfileScreen | Переключатель не связан |
| Вибрация | ProfileScreen | Переключатель не связан |

---

## Исправленные баги

### BUG-UI-001: Экран первого входа не соответствует дизайну
**Приоритет:** 🟡 Средний
**Статус:** ❌ Открыт

**Описание:**
При первом входе должен отображаться красивый экран с большой кнопкой "Начнем решать задачи!" - по нажатию на ней будет происходить авторегистрация пользователя.
Есть возможность раскрыть вход по email/пароль (если пользователь хочет попасть в свой аккаунт с другого устройства). В фоне должна быть картинка с Лежандром на диване.

**Место в коде:**
- `lib/presentation/screens/auth/login_screen.dart`

### BUG-COMM-001: Активные задачи показывались от других пользователей ✅
**Коммит:** 2c6d24d

**Проблема:**
В списке активных задач на домашнем экране отображались решения других пользователей. Пользователь мог "возобновить" чужую сессию.

**Решение:**
- Добавлен параметр `mineOnly` в `SolutionsRepository.getSolutions()`
- `getActiveSolutions()` теперь использует `mine_only=true`
- Добавлено поле `mineOnly` в `SolutionsFilter`

**Файлы:**
- `lib/data/repositories/solutions_repository.dart`
- `lib/presentation/providers/solutions_provider.dart`

---

### BUG-CONCEPT-001: Автоматический анализ навыков при просмотре решения ✅
**Коммит:** 3d98ce8

**Проблема:**
При открытии страницы решения автоматически вызывался анализ навыков через API `/concepts/analyze/solution/{id}`, что является платной функцией.

**Решение:**
- `solutionConceptsProvider` теперь вызывает `getSolutionConcepts()` вместо `analyzeSolution()`
- `problemConceptsProvider` теперь вызывает `getProblemConcepts()` вместо `analyzeProblem()`
- Добавлен метод `getProblemConcepts()` в ConceptsRepository

**Файлы:**
- `lib/presentation/providers/ocr_provider.dart`
- `lib/data/repositories/concepts_repository.dart`

---

### BUG-LIB-001: Сброс скролла при загрузке задач в Библиотеке ✅
**Коммит:** 7ae9b73

**Проблема:**
При нажатии "Загрузить ещё" позиция скролла сбрасывалась в начало списка.

**Решение:**
- Убран `_currentOffset` из state — offset вычисляется как `_accumulatedProblems.length`
- `ref.watch` всегда использует base filter с `offset: 0`
- Подгрузка через прямой API вызов без триггера rebuild
- Widget больше не пересоздаётся при load more

**Файлы:**
- `lib/presentation/screens/library/library_screen.dart`

---

### BUG-UI-003: Переполнение текста в карточке задачи ✅
**Коммит:** e58428e

**Проблема:**
Row с названием источника и именем автора не имел ограничений по ширине, вызывая `RenderFlex overflow`.

**Решение:**
- Обёрнуты тексты в `Flexible` с `overflow: TextOverflow.ellipsis`
- Добавлен `maxLines: 1`

**Файлы:**
- `lib/presentation/screens/library/library_screen.dart`

---

### BUG-UI-004: Переполнение в диалоге завершения задачи ✅
**Коммиты:** c3135a1, 8d2f967

**Проблема:**
Row с 5 звёздочками сложности вызывал `RenderFlex overflow` на узких экранах.

**Решение:**
- Заменён `IconButton` на `GestureDetector + Icon`
- Убраны встроенные отступы IconButton
- Использован `Expanded` для распределения пространства

**Файлы:**
- `lib/presentation/screens/solutions/dialogs/finish_session_sheet.dart`

---

### BUG-UI-005: Переполнение на главной странице ✅
**Коммит:** 5d1092c

**Проблема:**
Row со статистикой (стрик и задач сегодня) в карточке приветствия вызывал переполнение.

**Решение:**
- Обёрнуты тексты в `Flexible` с обрезкой

**Файлы:**
- `lib/presentation/screens/main_menu/home_screen.dart`

---

### BUG-LIB-002: Ограничение высоты карточки задачи ✅
**Коммит:** e3f142b

**Проблема:**
Карточки задач имели ограничение по высоте (12 строк, 300 символов), обрезая содержимое.

**Решение:**
- Убран `maxLines` из `MarkdownWithMath` и `Text`
- Убрана обрезка текста в `_previewText`
- Удалён неиспользуемый метод `_findSafeCutPoint`

**Файлы:**
- `lib/presentation/screens/library/library_screen.dart`

---

## Ранее исправленные баги

### BUG-PHOTO-001: Загрузка фото после создания задачи ✅
### BUG-LIST-001: Обновление списка задач после добавления ✅
### BUG-SESS-001: Кнопка "Сохранить ответ" неактивна ✅
### BUG-SESS-002: Условие задачи не показывается в сессии ✅
### BUG-SESS-003: Фото решения не прикрепляется к карточке ✅
### BUG-DISP-001: Просмотр фото условия при наличии текста ✅
### BUG-EDIT-001: Редактирование условия и тегов задачи ✅
### BUG-LATEX-001: Формулы в подсказках отображаются некорректно ✅
### BUG-SOL-001: Решение не показывается после финализации ✅
### BUG-OCR-001: OCR не предлагался после загрузки фото ✅
### BUG-DOLLAR-001: Символы $ в hintText вызывали ошибку ✅

---

## Новый функционал (последняя сессия)

### Изменение имени пользователя ✅
**Коммит:** f4a8dfa

**Реализовано:**
- Метод `updateProfile()` в AuthRepository (PATCH `/users/me`)
- Метод `updateProfile()` в AuthNotifier
- Кнопка редактирования (иконка карандаша) рядом с именем в профиле
- Диалог редактирования с валидацией

**Файлы:**
- `lib/data/repositories/auth_repository.dart`
- `lib/presentation/providers/auth_provider.dart`
- `lib/presentation/screens/profile/profile_screen.dart`

---

## Реализованный функционал

### Авторизация и профиль
- [x] Device login (автоматическое создание аккаунта по account_key)
- [x] Логин по email/пароль
- [x] Просмотр профиля
- [x] Конвертация анонимного аккаунта в постоянный
- [x] **Изменение имени пользователя**
- [x] Автоматическое переподключение через account_key

### Задачи (Problems)
- [x] Просмотр библиотеки задач с фильтрацией и пагинацией
- [x] Создание новой задачи
- [x] Просмотр деталей задачи
- [x] Редактирование текста условия
- [x] Редактирование тегов задачи
- [x] Загрузка фото условия
- [x] OCR условий с выбором AI-персон
- [x] Просмотр фото условия с zoom
- [x] **Карточки задач без ограничения высоты**

### Решения (Solutions)
- [x] Создание решения (начало сессии)
- [x] Интерактивная сессия с таймером
- [x] Завершение сессии с оценками сложности и качества
- [x] Продолжение активной сессии
- [x] Загрузка фото решения
- [x] OCR решений
- [x] Просмотр списка решений задачи
- [x] Редактирование текста решения
- [x] **Фильтрация активных решений по пользователю**

### Артефакты сессии
- [x] Создание озарений (epiphanies)
- [x] Создание вопросов (questions)
- [x] Ответы на вопросы вручную или через AI
- [x] Создание черновиков подсказок (hints)
- [x] AI-генерация подсказок
- [x] Просмотр всех артефактов

### Геймификация
- [x] Отображение XP
- [x] Статистика активности
- [x] Streak tracking
- [x] Мотивационные тексты
- [x] **Расширенная база мотивационных фраз**

### UI/UX
- [x] LaTeX поддержка (инлайн $...$ и display $$...$$)
- [x] Markdown рендеринг
- [x] Просмотр изображений с zoom
- [x] Выбор AI-персоны (Кот Базис, Петрович, Лежандр)
- [x] Тёмная/светлая тема (базовая)
- [x] **Исправлены все переполнения по ширине**

### Финансы
- [x] Просмотр баланса
- [x] Пополнение баланса через ЮKassa
- [x] История транзакций

---

## Нереализованный функционал (P2 - для следующих версий)

| Функция | Описание |
|---------|----------|
| Комментарии | Комментарии к задачам и решениям |
| Голосование | Лайки/дизлайки |
| Статьи | База знаний |

---

## Чек-лист для релиза в RuStore

### Критические (обязательно)
- [x] Иконка приложения (все размеры)
- [ ] Скриншоты для магазина (минимум 2)
- [ ] Описание приложения (краткое и полное)
- [ ] Политика конфиденциальности
- [ ] Release build APK
- [ ] Подписание APK

### Рекомендуемые
- [ ] Презентационное видео
- [ ] Категория приложения
- [ ] Возрастной рейтинг
- [ ] Ключевые слова для поиска
- [ ] Ссылка на сайт разработчика

### Технические
- [ ] Проверка на краши (debug build)
- [ ] Тестирование на разных размерах экранов
- [ ] Проверка работы без интернета
- [ ] Обработка ошибок API

## Инструкции для разработчиков

### Требования к среде
- Flutter SDK 3.x
- Dart 3.x
- Linux: GTK 3.0+, CMake, Ninja
- Android: Android SDK 21+

### Запуск проекта
```bash
# Клонирование (если нужно)
git clone <repo-url>
cd lezhandr

# Получить зависимости
flutter pub get

# Запуск на Linux
flutter run -d linux

# Запуск на Android
flutter run -d android

# Сборка release
flutter build linux --release
flutter build apk --release
```

### Правила создания моделей данных

**ВАЖНО:** Не использовать json_serializable для моделей API!

Причина: API может возвращать null для любых полей. Использовать ручную реализацию:

```dart
// ✅ Правильно
class MyModel {
  final int? id;
  final String name;

  MyModel({this.id, this.name = ''});

  factory MyModel.fromJson(Map<String, dynamic> json) {
    return MyModel(
      id: json['id'] as int?,
      name: json['name'] as String? ?? '',
    );
  }

  Map<String, dynamic> toJson() => {
    'id': id,
    'name': name,
  };
}

// ❌ Неправильно (создаст проблемы с null)
@JsonSerializable()
class MyModel {
  final int id;  // Упадёт если API вернёт null
  ...
}
```

### Добавление новых экранов

1. Создать файл в `lib/presentation/screens/<category>/<name>_screen.dart`
2. Добавить маршрут в `lib/core/router/app_router.dart`
3. Создать провайдеры данных в `lib/presentation/providers/`
4. Добавить импорт и тестировать

### Код-стайл

- Использовать `const` где возможно для оптимизации
- Nullable типы (`Type?`) для опциональных данных из API
- Default значения для обязательных полей в `fromJson`
- Геттеры для вычисляемых свойств (как `sourceName`)
- Документировать public API комментариями `///`

### Структура коммитов

Названия комитов предпочтительны на русском языке
```
feat: добавлена новая функция
fix: исправление бага
docs: обновление документации
refactor: рефакторинг без изменения функционала
style: форматирование кода
test: добавление тестов
```

---

## План дальнейшей разработки

### Фаза 1: Критические исправления
- [ ] **BUG-UI-001:** Улучшение экрана первого входа
- [ ] **BUG-COMM-001:** Фильтрация активных задач по пользователю (server-side)

### Фаза 2: Комьюнити (P2)
- [ ] Комментарии к задачам и решениям
- [ ] Лайки/дизлайки
- [ ] Статьи

### Фаза 3: Концепции и анализ знаний (P2)
- [ ] Экран списка концепций
- [ ] Экран детализации концепции
- [ ] Граф связей между концепциями
- [ ] AI-анализ задач и решений

### Фаза 4: Финансы (P2)
- [ ] UI пополнения баланса
- [ ] История транзакций

---


## Тестовые сценарии


---

## Известные проблемы и баги


### Критические (Требуют исправления)


---

## Ссылки и ресурсы

- **Оригинальный CLI клиент:** Python MindVector
- **Flutter документация:** https://docs.flutter.dev
- **Riverpod документация:** https://riverpod.dev
- **GoRouter документация:** https://pub.dev/packages/go_router
- **Material 3:** https://m3.material.io

---


*Этот документ должен быть источником истины для всех изменений в проекте.*
